# sandbox-conformance-py

Minimal Python repo with a pytest suite, used to verify the Hyrax dev
platform-sandbox (hyrax-services gateway) cutover: a fix/task job's test
phase should run `pytest` cross-account in the platform sandbox and return
a real result envelope.

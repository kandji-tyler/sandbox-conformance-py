# Run the full pytest test suite to confirm every test passes. Then add a one-lin…

**Tool:** `task`
**Severity:** unspecified

## What's wrong

Run the full pytest test suite to confirm every test passes. Then add a one-line comment at the very top of the primary module documenting that the suite was verified green, and run pytest once more to confirm it still passes after the edit.

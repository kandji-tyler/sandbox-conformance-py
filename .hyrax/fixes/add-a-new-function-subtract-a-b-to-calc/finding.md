# Add a new function subtract(a, b) to calc.py that returns a - b, with a one-lin…

**Tool:** `task`
**Severity:** unspecified

## What's wrong

Add a new function subtract(a, b) to calc.py that returns a - b, with a one-line docstring, matching the style of the existing add and mul functions. Do not modify the existing tests.
